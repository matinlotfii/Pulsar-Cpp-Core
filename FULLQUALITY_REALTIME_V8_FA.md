# Pulsar Full-Quality Realtime V8

## هدف این نسخه

این نسخه مسیر تصویر پزشکی و مسیر پیش‌نمایش UI را از هم جدا می‌کند تا مرورگر و JPEG نتوانند باعث ایجاد صف یا تأخیر در مانیتور و عینک‌ها شوند.

## مسیر اصلی مانیتور و عینک‌ها

1. دریافت کامل سنسور `4024x3036 BayerRG8` با پروفایل کارخانه/مرجع هر دوربین.
2. فقط دو بافر Acquisition و حالت `NewestOnly` برای جلوگیری از ماندن فریم قدیمی در صف.
3. کپی کوتاه به حافظه page-locked و سپس CUDA/NPP برای Debayer و Resize.
4. خروجی مناسب نمایشگر، بدون JPEG و بدون عبور از مرورگر.
5. OpenGL PBO سه‌گانه با ظرفیت قابل‌بازیافت و VSync خاموش.
6. جفت‌سازی `latest` بدون انتظار نرم‌افزاری برای فریم قدیمی.

## مسیر UI

- پیش‌نمایش مستقل `640x360`، حداکثر `12 fps` و JPEG quality `50`.
- Thread جدا با nice=12؛ هیچ اولویت realtime ندارد.
- فقط آخرین فریم در یک slot نگهداری می‌شود؛ صف پیش‌نمایش وجود ندارد.
- MJPEG مرورگر حذف شده است تا بافر داخلی Chrome باعث تأخیر نشود.
- UI با long-poll فقط فریمی جدیدتر از آخرین Frame ID می‌خواهد.
- Decode JPEG داخل Web Worker انجام می‌شود و جدیدترین Bitmap روی Canvas رسم می‌شود.
- هر درخواست پس از تحویل، cache پیش‌نمایش را آزاد می‌کند تا فریم قدیمی بعدی نمایش داده نشود.

## پاک‌سازی هر بار اجرا

`run.sh` سرویس و پردازش‌های قبلی Pulsar را متوقف می‌کند و سپس این موارد را حذف می‌کند:

- پروژه قبلی و Build آن
- `core/data` و browser profile
- تنظیمات محلی و display-routing قبلی
- cacheهای اختصاصی Pulsar
- سرویس user-level قدیمی dev-sync
- mirror قدیمی Git پروژه روی کامپیوتر مقصد

تنظیمات نمایشگر از نسخه قبلی Restore نمی‌شوند؛ نمایشگرها دوباره شناسایی می‌شوند و صفحه Display Settings با اسکریپت کامل V7 در دسترس است.

## لاگ سراسری

بعد از Deploy موفق، `run.sh` به مدت پیش‌فرض ۳۰ ثانیه یک Trace کم‌هزینه اجرا می‌کند. نمونه‌برداری یک‌بار در ثانیه است و هیچ `perf`، dump فریم یا log در حلقه هر پیکسل ندارد. Trace شامل این بخش‌هاست:

- FPS و وضعیت هر دوربین
- dequeue، raw copy، CUDA H2D/Debayer/Resize/D2H، publish
- سن فریم، skew استریو، texture upload و present
- resize/JPEG پیش‌نمایش و زمان تحویل latest-only UI
- CPU threadها، GPU، RAM، USB topology و process tree

فایل‌ها خودکار داخل پوشه `diagnostics/` همین پروژه روی کامپیوتر شخصی کپی می‌شوند.

## محدودیت فیزیکی

Exposure برابر `30000 us` است؛ بنابراین صفر میلی‌ثانیه تأخیر از نظر فیزیکی ممکن نیست. هدف این نسخه حذف صف و پردازش اضافه است. معیار عملی بسته، FPS نزدیک `32.3` و سن فریم نمایش حدود `35 تا 50 ms` است. Sync دقیق لحظه Exposure دو دوربین فقط با Trigger سخت‌افزاری مشترک قابل تضمین است.
