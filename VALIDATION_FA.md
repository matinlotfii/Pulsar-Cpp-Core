# نتیجه بررسی بسته

تاریخ بسته: 2026-08-06

موارد بررسی‌شده پیش از بسته‌بندی:

- صحت نحوی `run.sh`، `build-cpp.sh`، `dev-sync.sh` و `install-service.sh`
- Build کامل C++ در حالت CPU fallback تا 100٪ برای کنترل خطاهای کامپایل و لینک
- تشخیص صحیح نوع `STRING` یا `FILEPATH` برای `CMAKE_CUDA_COMPILER`
- اجبار CUDA Toolkit 13.2 و معماری RTX 3080 (`sm_86`) در Build ریموت
- حفظ `core/config/pulsar.local.env` موجود روی کامپیوتر پروژه هنگام Sync
- حذف ارجاع قدیمی سرویس به `pulsar-boot-preflight.sh` از Drop-In مدیریت‌شده
- بررسی Health سرویس و آنلاین‌شدن هر دو دوربین بعد از Deploy
- استفاده از UI از پیش Build‌شده برای جلوگیری از وابستگی به دانلود npm هنگام Deploy

بررسی واقعی CUDA، RTX 3080، دوربین‌های فیزیکی و نمایشگر توسط خود `./run.sh` روی
کامپیوتر پروژه انجام می‌شود و اگر Build CUDA، سرویس یا یکی از دو دوربین آماده نباشد،
فرمان با خطای روشن متوقف می‌شود.
