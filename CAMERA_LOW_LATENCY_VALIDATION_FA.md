# پروفایل دوربین و پردازش کم‌تأخیر Pulsar

این بسته رابط کاربری شاخهٔ جدید پروژه را نگه می‌دارد و فقط مسیر دوربین و نمایش را برای سیستم پروژه تنظیم می‌کند.

- دریافت واقعی هر دوربین از پروفایل GalaxyView با ابعاد کامل `4024x3036`، `BayerRG8`، نوردهی `30000us` و Gain صفر است.
- ابعاد `1920x1080` در `pulsar.env` ابعاد سنسور نیست؛ سقف خروجی RGB پس از Debayer/Resize روی GPU است. بنابراین کیفیت سنسور کامل حفظ می‌شود، ولی فریم RGB سی‌وشش‌مگابایتی بی‌دلیل به RAM و OpenGL منتقل نمی‌شود.
- بافر SDK روی `NewestOnly`، تعداد Acquisition روی ۲، انتقال USB روی ۱MiB/۲۰۰ URB و انتخاب آخرین فریم تنظیم شده است.
- ورودی SDK مستقیماً به CUDA منتقل می‌شود و در صورت خطا مسیر pinned-host به‌طور خودکار جایگزین می‌شود.
- Debayer و Resize با CUDA/NPP، آپلود با PBO، جفت‌سازی Stereo روی `latest` و VSync خاموش است.
- Renderer جدید یک پنجره/هدف RTX برای خروجی‌های فعال می‌سازد و هر فریم را فقط یک‌بار Upload می‌کند.
- هنگام Deploy فقط Overrideهای قدیمی دوربین از `pulsar.local.env` حذف می‌شوند؛ تنظیمات نمایشگر، لمس، شبکه و صدا باقی می‌مانند و قبل از تغییر Backup ساخته می‌شود.

`./run.sh` روی کامپیوتر شخصی Build نمی‌کند. ابتدا Commit/Push و Backup انجام می‌شود، سپس سورس به `matin@192.168.1.123` منتقل، Build تمیز CUDA 13.2 اجرا، سرویس Restart و آنلاین بودن هر دو دوربین بررسی می‌شود.
