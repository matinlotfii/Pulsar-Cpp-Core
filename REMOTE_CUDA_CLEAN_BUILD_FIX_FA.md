# اصلاح Build ریموت CUDA

این نسخه هنگام اجرای پیش‌فرض `./run.sh` روی کامپیوتر شخصی، قبل از Build روی کامپیوتر پروژه، پوشه `core/build` ریموت را پاک می‌کند و CMake/CUDA را از ابتدا می‌سازد.

علت: `rsync` زمان فایل‌ها را حفظ می‌کند و پوشه Build ریموت هم Sync نمی‌شود. در نسخه قبلی ممکن بود فایل شیء قدیمی `GpuBayerPipeline.cu.o` باقی بماند و با وجود تعریف شدن متدهای جدید در سورس، Linker خطای `undefined reference` بدهد.

روند پیش‌فرض:

1. روی کامپیوتر شخصی Build انجام نمی‌شود.
2. تغییرات Commit و به GitHub Push می‌شوند.
3. سورس به `matin@192.168.1.123:/home/matin/Pulsar-Cpp-Core` منتقل می‌شود.
4. Build ریموت به‌صورت Clean با CUDA 13.2 و معماری RTX 3080 (`sm_86`) انجام می‌شود.
5. سرویس Restart می‌شود و Health و آنلاین بودن هر دو دوربین بررسی می‌شود.

تنظیم محلی `core/config/pulsar.local.env` روی کامپیوتر پروژه Sync یا حذف نمی‌شود.
